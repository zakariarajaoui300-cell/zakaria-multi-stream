import telebot
import os

# التوكن الجديد والمفعل الخاص بك
API_TOKEN = "8277065517:AAEpbvbm-gcOsgKj1qyx2vYbOWJ3gZ2FJ3c"

bot = telebot.TeleBot(API_TOKEN)

# رسالة الترحيب عند إرسال /start
@bot.message_handler(commands=['start'])
def send_welcome(message):
    welcome_text = (
        "أهلاً بك يا زكريا! ⚡\n\n"
        "بوت البث المباشر (ZakariaStreamBot) يعمل الآن بنجاح.\n"
        "جاهز لاستقبال أوامرك الآن."
    )
    bot.reply_to(message, welcome_text)

# الرد التلقائي على أي رسالة أخرى للتجربة
@bot.message_handler(func=lambda message: True)
def echo_all(message):
    bot.reply_to(message, f"لقد استلمت رسالتك: {message.text}")

if __name__ == "__main__":
    print("Bot is starting...")
    bot.infinity_polling()
